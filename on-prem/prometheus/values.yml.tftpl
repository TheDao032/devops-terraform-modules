grafana:
  grafana.ini:
      server:
        root_url: "%(protocol)s://%(domain)s:%(http_port)s/grafana"

  adminPassword: ${grafana.auth.password}

prometheus:
  prometheusSpec:
    externalUrl: ///prometheus
